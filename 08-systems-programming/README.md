#include <fcntl.h>   // For open() flags like O_WRONLY, O_CREAT
#include <unistd.h> 
    ssize_t


size_t, void *, Structs, enum, typedef, const, static ssize_t
(malloc, calloc, realloc, free)
pid_t =, errno
File descriptors